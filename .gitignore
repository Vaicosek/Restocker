# ---- Secrets & auth — never commit (token + live dashboard sessions) ----
.env
env
*.env
.env.*
Mconfig.yml
data/state/Mconfig.yml
data/state/web_sessions.yml
data/state/web_login_codes.yml

# ---- Environment / virtualenv / caches (not useful in git) ----
.venv/
venv/
__pycache__/
__pycache_/
*.pyc
*.pyo
.cache/
.local/

# ---- Logs ----
*.log

# ---- Large OS-specific binaries (upload per-host; 38-52 MB each) ----
cloudflared
cloudflared.exe
*.exe

# ---- Editor / IDE ----
.idea/
.vscode/
*.swp
.DS_Store

# ---- Live database + bot-written state — CANNOT be tracked in git ----
# The running bot writes these constantly, so every `git pull` conflicts and aborts.
# Keep them per-host (upload manually / reinstall). Code still syncs via git fine.
restocker.db
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
data/
csn_stock_MERGED.csv
