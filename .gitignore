# ---- Secrets & auth — never commit (token + live dashboard sessions) ----
.env
env
*.env
.env.*
Mconfig.yml
data/state/Mconfig.yml
data/state/web_sessions.yml
data/state/web_login_codes.yml

# ---- Environment / virtualenv / caches (not useful in git) ----
.venv/
venv/
__pycache__/
__pycache_/
*.pyc
*.pyo
.cache/
.local/

# ---- Logs ----
*.log

# ---- Large OS-specific binaries (upload per-host; 38-52 MB each) ----
cloudflared
cloudflared.exe
*.exe

# ---- Editor / IDE ----
.idea/
.vscode/
*.swp
.DS_Store

# NOTE: restocker.db, data/, *.yml and *.csv ARE tracked on purpose
# (public Minecraft economy data). Only .env / env stay out.
