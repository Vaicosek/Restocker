# Pinned to major versions to prevent a breaking/compromised new major from being
# auto-adopted on deploy (supply-chain hardening). For full reproducibility run
# `pip freeze > requirements.lock` on the working server and deploy from that.
discord.py>=2.4,<3
python-dotenv>=1.0,<2
pyyaml>=6.0,<7
anthropic>=0.39,<1
aiohttp>=3.9,<4
openpyxl>=3.1,<4   # /import_earnings reads .xlsx files
